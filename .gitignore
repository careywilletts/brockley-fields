# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
# Ignore every .env variant, not just *.local, so a stray `.env` or
# `.env.production` can never be committed to what is a public repo.
.env
.env.*
!.env.example

# Common ignores
node_modules
.next/
.DS_Store
